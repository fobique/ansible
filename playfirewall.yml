---
- name: Firewall Power Off
  hosts: haproxy
  become: yes

  tasks:
  - name: Disable SELinux
    selinux:
     state: disabled

  - name: Disable firewall
    service:
     name: firewalld
     state: stopped  
     enabled: no
